# Model Switching

```bash
# Download a different model if not present
voxtype setup model  # Interactive selection

# Or specify directly
voxtype setup model --set small.en

# Test with different models (edit config.toml or use --model flag)
```

